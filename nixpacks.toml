[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = [
  "cd server && python3 -m venv .venv && .venv/bin/pip install -r requirements.txt"
]

[phases.build]
cmds = []

[phases.start]
cmd = "cd server && .venv/bin/uvicorn main:app --host 0.0.0.0 --port $PORT"
